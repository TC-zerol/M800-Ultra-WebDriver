# 多彩 M800 Ultra (DELUX Receiver 320F:225B) HID 配置协议

逆向来源：hid_monitor.py 监听官方驱动通信（2026-09-22 抓包 `capture/events.log`），
并经实机收发验证（读电量/读配置/写配置全部成功）。

## 通道

- 复合设备 MI_01 上的 vendor-defined 集合：usagePage=0xFF1C, usage=0x92
- 输入/输出报告均 64 字节，reportId=0x04
- WebHID 可访问（浏览器选择 DELUX Receiver 即可）

## 报告格式（64 字节）

```
[0]     = 0x04                reportId
[1..2]  = CRC16-MODBUS(字节[3..31])  小端；电量(0x1A)/ACK(0xAA) 包为 00 00
[3]     = 命令
[4]     = 长度（配置块固定 0x18=24）
[5]     = 偏移（0x00/0x18/0x30/0x48）
[6..7]  = 00 00
[8..31] = 24 字节载荷
[32..63]= 00 填充
```

## 命令

| cmd  | 含义       | 说明 |
|------|-----------|------|
| 0x1A | 电量查询   | 响应载荷[0]=百分比；官方驱动每 ~3 秒轮询一次 |
| 0x05 | 读配置块   | len=0x18, off=块偏移；响应同 cmd，含 24 字节数据 |
| 0x06 | 写配置块   | 同上布局；写入立即生效，设备会广播 06 同步包 |
| 0x03 | 版本信息   | 载荷: `aa 55 00 00 01 0c 18 00 01 95 33 32 80 3e 64 00 01 05 01 07`（字段未完全解析） |
| 0x07 | 按键映射   | 3 字节/键×7：(10 01 00)=左键 (10 02 00)=右键 (10 04 00)=中键 (10 08 00)=后退 (10 10 00)=前进 (13 03 00)/(15 03 00)=DPI 键 |
| 0x01/0x02 | 灯光配置 | 与块 0x48 内容相同 |
| 0xAA | ACK/心跳   | `aa 00 aa 00 00 00 00 ff` |

## 配置块映射（24 字节载荷内偏移）

### DPI 档位记录 —— 跨块连续的 9 字节结构（已完全破解 ✅）

四个配置块（0x00/0x18/0x30/0x48）拼接成连续流，6 个 DPI 档位各占 9 字节，
从流偏移 14 开始：`[启用, 00, X_L, X_H, Y_L, Y_H, R, G, B]`

| 档位 | 流偏移 | 启用字节 | DPI X | DPI Y | 指示灯 RGB |
|------|--------|----------|-------|-------|-----------|
| 1 | 14 | 块0x00[14] | 块0x00[16..17] | 块0x00[18..19] | 块0x00[20..22] |
| 2 | 23 | 块0x00[23] | 块0x18[1..2]  | 块0x18[3..4]  | 块0x18[5..7] |
| 3 | 32 | 块0x18[8]  | 块0x18[10..11]| 块0x18[12..13]| 块0x18[14..16] |
| 4 | 41 | 块0x18[17] | 块0x18[19..20]| 块0x18[21..22]| 块0x18[23]+块0x30[0..1]（跨块）|
| 5 | 50 | 块0x30[2]  | 块0x30[4..5]  | 块0x30[6..7]  | 块0x30[8..10] |
| 6 | 59 | 块0x30[11] | 块0x30[13..14]| 块0x30[15..16]| 块0x30[17..19] |

- 启用字节：1=启用 0=禁用（实机验证：官方驱动关闭 4/5/6 档后，恰好这三个字节 01→00）
- 官方 UI 单值模式（StageXY=0）下 X/Y 写同一值；出厂默认 Y 与 X 不同（如档1 X=400/Y=1600）
- 默认颜色：红/绿/蓝/品红/黄/白，与块 0x48 的灯光区一致

### 块 0x00 — 基本参数
```
[2]     有线回报率档位        [3]   官方驱动的"休眠设置"下拉索引（驱动本地记忆，设备休眠时间以 0x30[22..23] 为准）
[5]     常数 01（曾猜为深度休眠开关，实机对比证伪：切换深度休眠时不变）
[6]     常数 02（曾猜为按键延时，实机对比证伪：改按键延时时不变）
[10]    直线校准开关（1=ON 0=OFF，实机对比验证 ✅）
[11]    无线回报率档位: 0=125 1=250 2=500 3=1000（官方 UI 仅提供这四档）
[13]    静默高度 LOD（0=1MM 1=2MM，实机对比验证 ✅）
[14..22] 档位1 记录（见上表）  [23]  档位2 启用字节
```

### 块 0x30 — 档位5-6 + 休眠
```
[20]=浅休眠秒数(默认60, json sleeptime，官方 UI 未暴露)  [21]=高位(默认0)
[22..23]="休眠设置"秒数 LE（默认600=10分钟；官方下拉 3/4/5/10/15/20 分钟 → 180/240/300/600/900/1200，实机验证 ✅）
```

### 块 0x48 — 按键延时/开关 + DPI 灯色 + 镜像
```
[0]     按键延时 = 毫秒数-1（官方下拉 2/4/6/8/10ms → 存 1/3/5/7/9，实机验证 ✅）
[1]     常数 00
[2]     移动同步开关（1=ON 0=OFF，实机对比验证 ✅）
[3..20] 6 个 DPI 档位指示灯 RGB（红/绿/蓝/品红/黄/白，3 字节×6）
[21]    深度休眠开关（1=ON 0=OFF，实机对比验证 ✅）
[22]    有线回报率镜像    [23]=无线回报率镜像（官方驱动改回报率时会同步写）
```

### "其他设置"字段速查（2026-09-22 实机对比法逆向，全部验证）

| 官方 UI 项 | 位置 | 编码 |
|-----------|------|------|
| 静默高度 | 0x00[13] | 0=1MM, 1=2MM |
| 直线校准 | 0x00[10] | 1=ON, 0=OFF |
| 移动同步 | 0x48[2] | 1=ON, 0=OFF |
| 深度休眠 | 0x48[21] | 1=ON, 0=OFF |
| 休眠设置 | 0x30[22..23] | 秒数 LE（分钟×60） |
| 按键延时 | 0x48[0] | 毫秒数-1 |

## 备注 / 未解之谜

- 电量包（cmd=0x1A）没有 len/off 头，布局与配置包不同（充/放电双状态实机确认）：
  `04 00 00 1a 00 00 00 ff [百分比] [充电标记]` —— 第7字节 FF 是固定标记（不是充电标志！），
  充电标记 01=充电 / 00=放电；充电中设备约每 2 秒主动广播一次。
- 电量百分比下降速度曾异常偏快（3 分钟 56%→36%），可能上电校准期。网页版已加历史图表可长期观察。
- 档位 4/5/6 的启用字节（0x18[17] / 0x30[2] / 0x30[11]）在全启用状态下读到 00，
  而档位 1/2/3 读到 01——行为不一致，疑似还有未理解的语义，待后续实测勾选行为确认。
- 配置中没有"当前档位"字段——官方 UI 的当前档位高亮是驱动本地状态（Mouse.json 的 StageActive），
  设备端无法读取当前档位。
- 块 0x00 [1]=05、[8]=ff、[12]=01、[15]=00 含义未知。
- 写入是否掉电持久化未验证（写入后跨进程读回一致）。
- 报告头为固定 8 字节：`[0]=04 [1..2]=CRC [3]=cmd [4]=len [5]=off [6..7]=00 00`，载荷从 [8] 开始（早期记录曾误数成 3 个填充字节，以本文档为准）。
- 若配置被写乱：用官方驱动恢复默认即可。
